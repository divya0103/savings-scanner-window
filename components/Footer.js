import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const footer= (props) => {
    return(
        <View>
        
        {props.children}
        

        </View>
    );

};
export default footer;